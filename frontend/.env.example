# Frontend environment configuration (Vite exposes only VITE_* prefixed vars)
# Copy to ".env" for local development.

# Base URL of the backend API. Leave as-is when using the Vite dev proxy.
VITE_API_BASE_URL=/api
